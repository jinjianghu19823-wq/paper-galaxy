# Randomized SVD

Randomized singular value decomposition builds a low-dimensional sketch of a
matrix before computing a compact factorization. Power iterations, oversampling,
orthogonality checks, and residual estimates help make the approximation stable
for numerical linear algebra experiments.
